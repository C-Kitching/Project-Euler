
def find_upper_bound(N):
    """Find upper bound on the number that could be the sum of its digits,
    each raised to the Nth power

    Args:
        N (int): power to raise digits to
    """


N = int(input())
upper_bound = find_upper_bound(N)